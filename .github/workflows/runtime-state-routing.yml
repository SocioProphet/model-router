name: runtime-state-routing

on:
  pull_request:
    branches: [ main ]
    paths:
      - "examples/runtime-state-routing-policy.*.json"
      - "tools/validate_runtime_state_routing.py"
      - ".github/workflows/runtime-state-routing.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-runtime-state-routing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Validate runtime state routing fixtures
        run: python3 tools/validate_runtime_state_routing.py
